apiVersion: v1
kind: Pod
metadata:
  name: tomcat
spec:
  containers:
  - name: tomcat
    image: tomcat:8.0
    resources:
      limits:
        cpu: "0.25"
        memory: "16Mi"
      requests:
        cpu: "0.25"
        memory: "8Mi"
    ports:
    - containerPort: 7500
    imagePullPolicy: Always
