apiVersion: v1
kind: Pod
metadata:
  name: Tomcat
spec:
  containers:
  - name: tomcat
    image: tomcat:8.0
    resources:
      limits:
        cpu: "0.25"
        memory: "100Mi"
      requests:
        cpu: "0.25"
        memory: "80Mi"
    ports:
    - containerPort: 7500
    imagePullPolicy: Always
